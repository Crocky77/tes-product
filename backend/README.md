# TES Backend (Phase B.1 Skeleton)

Minimal Node backend skeleton for CHPP integration.

## What is included in B.1

- HTTP server entrypoint (`src/server.js`)
- `GET /health` endpoint
- basic root endpoint (`GET /`) to confirm service status
- `.env.example` placeholder for upcoming OAuth variables

## Run

```bash
cd backend
npm start
